import React from 'react'
import styles from "../../styles/por"

const PortfolioSection = ({ref}: {ref: React.RefObject<HTMLDivElement | null>;}) => {
  return (
    <div ref={ref}>PortfolioSection</div>
  )
}

export default PortfolioSection