"use client";

import React, {useRef} from 'react'
import gsap from "gsap"
import { useGSAP } from '@gsap/react';

const Copy = ({children}) => {
  return (
    <div>Copy</div>
  )
}

export default Copy